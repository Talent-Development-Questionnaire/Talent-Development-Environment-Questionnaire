<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TDQ.GroupPage"
             x:Name="GroupContentPage">
    <ContentPage.Content>
        <StackLayout>      
            <!-- Later on when binding data, outer Grid to be made a DataTemplate for a CollectionView -->
            <ListView x:Name="LstViewGroup" 
                      Margin="20"
                      ItemSelected="LstViewGroup_ItemSelected"
                      HasUnevenRows="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal"
                                         Padding="5">
                                <Image Source="{Binding ImageFilePath}"
                                       Aspect="AspectFill"
                                       WidthRequest="100"
                                       HeightRequest="100"/>
                                <StackLayout>
                                    <Label Text="{Binding Name}" TextColor="Black" FontSize="Large"/>
                                    <StackLayout Orientation="Horizontal">
                                    <Label Text=" No. of Members: " TextColor="DarkGray" FontSize="Medium"/>
                                    <Label Text="{Binding GroupNo}" TextColor="DarkGray" FontSize="Medium"/>
                                    </StackLayout>
                                </StackLayout>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
                <ImageButton x:Name="ImgBtnAddGroup"    
                    Source="ic_add_circle_outline.png"
                    HorizontalOptions="Center"
                    VerticalOptions="EndAndExpand"
                    BackgroundColor="Transparent"
                    Margin="0,0,0,10"
                    Clicked="ImgBtnAddGroup_Clicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>